# CVS Data Load
A repository containing data load scripts written in python.  These are used to load data into DynamoDB to test various data scenarios.

These scripts rely on the local AWS CLI credentials being set-up correctly, see [here](https://docs.aws.amazon.com/cli/latest/userguide/cli-chap-getting-started.html) for more details.
